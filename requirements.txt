# requirements.txt
# Ensure build tooling is available for PEP 517 builds
setuptools>=65.5.0
wheel
# Allow newer pip if Render's environment needs it
pip>=23.0

flask==2.3.3
flask-cors==4.0.0
joblib==1.3.2
# Use a compatible numpy wheel range to avoid source builds on some platforms
numpy>=1.24,<1.26
# Compatible pandas and scikit-learn ranges
pandas>=2.0,<2.1
scikit-learn>=1.3,<1.5

gunicorn==21.2.0